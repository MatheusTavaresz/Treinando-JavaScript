var primeiroNumero = parseInt (prompt('Digite o primeiro número'))
var segundoNumero = parseInt  (prompt('Digite o segundo número'))
var resultadoSub = (primeiroNumero - segundoNumero)
  console.warn(resultadoSub)
  var resultadoSoma = primeiroNumero + segundoNumero
  console.warn (resultadoSoma)
  var resultadoMulti = (primeiroNumero * segundoNumero)
  console.warn (resultadoMulti)
  var resultadoDivisao = (primeiroNumero / segundoNumero)
  console.warn (resultadoDivisao);
